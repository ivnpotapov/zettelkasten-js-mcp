/**
 * SQLite index module
 *
 * Provides efficient querying and indexing of notes using SQLite database.
 *
 * @module infrastructure/persistence/sqlite-index
 */

export { SqliteIndex } from "./sqlite-index.js";
export type {
  ISqliteIndex,
  SqliteIndexConfig,
} from "./types.js";
